@model TestRazorPages.Models.User?
@if (Model != null)
{
    <h3>Featured User</h3>
    <div>
        <strong>@Model.Name</strong><br/>
        @Model.Website<br/>
        @Model.Company.Name<br/>
    </div>
}